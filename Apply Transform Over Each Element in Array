/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    const response  = [];
    for(const i in arr) {
        response.push(fn(arr[i], Number(i)));
    }
    return response;
};
